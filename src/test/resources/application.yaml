spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mariadb://localhost:3306/forumhubtest}
    username: ${DATABASE_USER:root}
    password: ${DATABASE_PASSWORD:root}
    driver-class-name: org.mariadb.jdbc.Driver
